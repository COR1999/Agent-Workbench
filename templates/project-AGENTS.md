# <Project> — agent context

## What this is

<Two or three sentences. What the project is, who it is for, and what stage it
is at. Enough that a session starting cold does not have to re-derive it.>

## Stack

<Only what is load-bearing, and only what is not obvious from package.json.
Versions where a version matters. Anything deliberately pinned, with the reason.>

## Conventions

<Only what an agent would get wrong by inference. Delete any line that reading
two files would already tell you.>

- Files:
- Components:
- User-facing copy lives in:
- Colours/tokens live in:
- Constants live in:
- Error handling:

## Rules

<Project-specific only. Things that are true here and would be false or
meaningless elsewhere. Machine-wide rules live in personal-agent-system/AGENTS.md.>

## Inherited lessons

<!-- from personal-agent-system/lessons — edit the source, not this copy -->

- **<slug>** (<YYYY-MM>) — <one-line claim>

## Settled decisions

<What has already been decided and should not be re-litigated. One line each,
with the reason. Add to this rather than rewriting it.>
